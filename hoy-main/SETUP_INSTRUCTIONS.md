# 📋 คำแนะนำการตั้งค่าและใช้งาน

## 1. 🔧 Database Migration

ก่อนใช้งาน ต้องรัน SQL migration เพื่อเพิ่มคอลัมน์สำหรับ Email Verification:

```sql
-- ไฟล์: backend/migrations/add_email_verification.sql

-- เพิ่มคอลัมน์ verification_token และ token_expiry
ALTER TABLE users 
ADD COLUMN verification_token VARCHAR(255) NULL,
ADD COLUMN token_expiry DATETIME NULL;

-- อัปเดต status enum ให้รองรับ 'pending'
ALTER TABLE users 
MODIFY COLUMN status ENUM('active', 'inactive', 'pending') DEFAULT 'pending';

-- เพิ่ม index สำหรับการค้นหาที่เร็วขึ้น
CREATE INDEX idx_verification_token ON users(verification_token);
CREATE INDEX idx_email_status ON users(email, status);
```

**วิธีรัน:**
1. เปิด phpMyAdmin
2. เลือก database `rga_dashboard`
3. คลิก SQL tab
4. Copy-paste SQL จากไฟล์ `backend/migrations/add_email_verification.sql`
5. คลิก Go

---

## 2. ✅ ฟีเจอร์ที่สร้างเสร็จแล้ว

### 1. Email Verification System
- ✅ สมัครสมาชิกพร้อม email verification
- ✅ หน้า Verify Email (`/verify/:token`)
- ✅ Resend verification email
- ✅ Login ตรวจสอบว่า email verified แล้วหรือยัง

**API Endpoints:**
- `POST /api/auth/register` - สมัครสมาชิก (จะส่ง verification token)
- `GET /api/auth/verify/:token` - ยืนยันอีเมล
- `POST /api/auth/resend-verification` - ส่งอีเมลยืนยันอีกครั้ง

### 2. Admin Dashboard
- ✅ หน้า Admin Dashboard แยกจาก User Dashboard (`/admin`)
- ✅ แสดงสถิติทั้งหมด (Users, Clients, Orders, Revenue)
- ✅ แสดงผู้ใช้ที่รอยืนยันอีเมล
- ✅ Quick links สำหรับจัดการผู้ใช้และลูกค้า

### 3. User Profile Management
- ✅ หน้า Profile (`/profile`)
- ✅ แก้ไขชื่อและอีเมล
- ✅ เปลี่ยนรหัสผ่าน
- ✅ เปลี่ยน Role เป็น Admin ได้ (สำหรับผู้ใช้)

**API Endpoints:**
- `GET /api/users/profile` - ดึงข้อมูลโปรไฟล์
- `PUT /api/users/profile` - อัปเดตโปรไฟล์
- `PUT /api/users/profile/role` - เปลี่ยน Role

### 4. Admin User Management
- ✅ Admin สามารถดูผู้ใช้ทั้งหมดได้
- ✅ Admin สามารถแก้ไขข้อมูลผู้ใช้ได้

**API Endpoints:**
- `GET /api/users` - ดึงผู้ใช้ทั้งหมด (Admin only)
- `PUT /api/users/:id` - อัปเดตผู้ใช้ (Admin only)

---

## 3. 🎯 การใช้งาน

### การสมัครสมาชิก
1. ไปที่ `/register`
2. กรอกข้อมูล (ชื่อ, อีเมล, รหัสผ่าน)
3. หลังจากสมัครสำเร็จ จะได้ verification token
4. คลิกที่ลิงก์ยืนยัน หรือไปที่ `/verify/:token`
5. หลังจากยืนยันแล้ว จะได้ token และ redirect ไป dashboard

### การเปลี่ยน Role เป็น Admin
1. ไปที่ `/profile`
2. คลิกปุ่ม "เปลี่ยนเป็น Admin"
3. ยืนยันการเปลี่ยน
4. ระบบจะ refresh และแสดง Admin Dashboard

### Admin Dashboard
- Admin จะถูก redirect ไป `/admin` อัตโนมัติเมื่อเข้า `/dashboard`
- Admin สามารถดูข้อมูลทั้งหมดได้
- Admin สามารถจัดการผู้ใช้และลูกค้าได้

---

## 4. 📝 หมายเหตุ

### Email Service (Production)
ปัจจุบันระบบจะแสดง verification token ใน console และ response (สำหรับ development)

**สำหรับ Production ต้อง:**
1. ติดตั้ง email service (เช่น nodemailer, SendGrid, AWS SES)
2. สร้าง email template
3. ส่ง verification email จริงๆ แทนการแสดง token

**ตัวอย่างโค้ดที่จะต้องเพิ่ม:**
```javascript
// ใน emailVerificationController.js
const nodemailer = require('nodemailer');

const transporter = nodemailer.createTransport({
  // email service config
});

await transporter.sendMail({
  to: email,
  subject: 'Verify your email',
  html: `<a href="${verificationUrl}">Click here to verify</a>`
});
```

### Security Notes
- การเปลี่ยน Role เป็น Admin ควรมี security check เพิ่มเติม (เช่น ต้องมี approval หรือ admin password)
- Verification token มีอายุ 24 ชั่วโมง
- Password ต้องมีอย่างน้อย 6 ตัวอักษร

---

## 5. 🚀 Next Steps

1. **รัน Database Migration** - รัน SQL migration ก่อน
2. **ทดสอบ Email Verification** - สมัครสมาชิกและยืนยันอีเมล
3. **ทดสอบ Admin Dashboard** - เปลี่ยน Role เป็น Admin และดู Admin Dashboard
4. **ทดสอบ Profile Management** - แก้ไขข้อมูลและเปลี่ยนรหัสผ่าน

---

## 6. 📞 Troubleshooting

### ปัญหา: ไม่สามารถยืนยันอีเมลได้
- ตรวจสอบว่า database migration รันแล้ว
- ตรวจสอบว่า token ยังไม่หมดอายุ (24 ชั่วโมง)

### ปัญหา: ไม่สามารถเปลี่ยน Role ได้
- ตรวจสอบว่า user มีสิทธิ์เข้าถึง API
- ตรวจสอบว่า backend server รันอยู่

### ปัญหา: Admin Dashboard ไม่แสดง
- ตรวจสอบว่า user.role === 'admin'
- ตรวจสอบว่า route `/admin` ถูกเพิ่มใน App.jsx แล้ว


