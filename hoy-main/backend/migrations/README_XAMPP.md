# 📋 คำแนะนำการรัน Migration ใน XAMPP

## วิธีรัน SQL Migration ใน phpMyAdmin

### ขั้นตอนที่ 1: เปิด phpMyAdmin
1. เปิดเบราว์เซอร์ (Chrome, Firefox, etc.)
2. ไปที่ `http://localhost/phpmyadmin`
3. Login ด้วย username: `root` (password: ว่างเปล่า หรือตามที่ตั้งไว้)

### ขั้นตอนที่ 2: เลือก Database
1. คลิกที่ชื่อ database `rga_dashboard` ใน sidebar ด้านซ้าย
2. ถ้ายังไม่มี database ให้สร้างใหม่:
   - คลิก "New" หรือ "สร้างใหม่"
   - ตั้งชื่อ database: `rga_dashboard`
   - เลือก Collation: `utf8mb4_general_ci`
   - คลิก "Create" หรือ "สร้าง"

### ขั้นตอนที่ 3: รัน SQL Migration
1. คลิกที่ tab "SQL" ด้านบน
2. Copy เนื้อหาทั้งหมดจากไฟล์ `add_email_verification.sql`
3. Paste ลงในช่อง SQL
4. คลิกปุ่ม "Go" หรือ "ไป"

### ขั้นตอนที่ 4: ตรวจสอบผลลัพธ์
- ถ้าสำเร็จ จะเห็นข้อความ "Success" หรือ "สำเร็จ"
- ถ้ามี error:
  - ถ้า error ว่า "Duplicate column name" = คอลัมน์มีอยู่แล้ว (ไม่เป็นไร)
  - ถ้า error ว่า "Duplicate key name" = index มีอยู่แล้ว (ไม่เป็นไร)
  - ถ้า error อื่นๆ ให้อ่าน error message

---

## วิธีรันทีละคำสั่ง (ถ้ามีปัญหา)

ถ้ารันพร้อมกันไม่ได้ ให้รันทีละคำสั่ง:

### 1. เพิ่มคอลัมน์ verification_token
```sql
ALTER TABLE users 
ADD COLUMN verification_token VARCHAR(255) NULL;
```

### 2. เพิ่มคอลัมน์ token_expiry
```sql
ALTER TABLE users 
ADD COLUMN token_expiry DATETIME NULL;
```

### 3. อัปเดต status enum
```sql
ALTER TABLE users 
MODIFY COLUMN status ENUM('active', 'inactive', 'pending') DEFAULT 'pending';
```

### 4. เพิ่ม Index
```sql
CREATE INDEX idx_verification_token ON users(verification_token);
CREATE INDEX idx_email_status ON users(email, status);
```

---

## ตรวจสอบว่าตารางอัปเดตแล้วหรือยัง

รันคำสั่งนี้ใน SQL tab:

```sql
DESCRIBE users;
```

ควรเห็นคอลัมน์:
- `verification_token` (VARCHAR(255), NULL)
- `token_expiry` (DATETIME, NULL)
- `status` (ENUM('active', 'inactive', 'pending'))

---

## Troubleshooting

### ปัญหา: Error "Duplicate column name 'verification_token'"
**แก้ไข:** คอลัมน์มีอยู่แล้ว ไม่ต้องทำอะไร เพียงรันคำสั่งถัดไป

### ปัญหา: Error เมื่อ modify status enum
**แก้ไข:** 
1. ตรวจสอบว่ามีข้อมูลในตาราง users หรือไม่
2. ถ้ามีข้อมูล ให้อัปเดตข้อมูลที่มี status ที่ไม่ใช่ 'pending' ก่อน
3. หรือรันคำสั่งนี้ก่อน:
```sql
UPDATE users SET status = 'active' WHERE status IS NULL OR status = '';
```

### ปัญหา: Error "Duplicate key name"
**แก้ไข:** Index มีอยู่แล้ว ไม่ต้องทำอะไร

---

## ตัวอย่างการใช้งานหลังจาก Migration

หลังจาก migration สำเร็จแล้ว คุณสามารถ:

1. **สมัครสมาชิกใหม่** - จะได้ status = 'pending'
2. **ยืนยันอีเมล** - จะเปลี่ยน status = 'active'
3. **Login** - ระบบจะตรวจสอบว่า status = 'active' หรือไม่

---

## ตรวจสอบข้อมูลในตาราง

```sql
-- ดูข้อมูลผู้ใช้ทั้งหมด
SELECT id, name, email, role, status, verification_token, token_expiry FROM users;

-- ดูผู้ใช้ที่รอยืนยันอีเมล
SELECT id, name, email, status FROM users WHERE status = 'pending';

-- ดูผู้ใช้ที่ยืนยันแล้ว
SELECT id, name, email, status FROM users WHERE status = 'active';
```


