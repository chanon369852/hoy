# 📊 RGA Dashboard System - เอกสารอธิบายระบบ

## 📋 สารบัญ
1. [ภาพรวมระบบ](#ภาพรวมระบบ)
2. [Dashboard Overview](#dashboard-overview)
3. [AI Agent Features](#ai-agent-features)
4. [Reports System](#reports-system)
5. [Settings Configuration](#settings-configuration)
6. [E-commerce Integration](#e-commerce-integration)
7. [Data Integration](#data-integration)
8. [Backend APIs](#backend-apis)
9. [Frontend Components](#frontend-components)
10. [การใช้งาน](#การใช้งาน)

---

## 🎯 ภาพรวมระบบ

ระบบ Dashboard สำหรับ RGA (Rise Group Asia) เป็นระบบ Multi-tenant ที่ให้ลูกค้าแต่ละรายสามารถ:
- ดูข้อมูลการโฆษณาของตนเองเท่านั้น
- วิเคราะห์ประสิทธิภาพแคมเปญ
- รับคำแนะนำจาก AI
- สร้างรายงานและส่งออก
- ตั้งค่า threshold และการแจ้งเตือน

---

## 📊 Dashboard Overview

### ไฟล์: `frontend-react/src/pages/DashboardEnhanced.jsx`

### สิ่งที่แสดง:

#### 1. **KPI Cards (8 การ์ด)**
แสดงเมตริกหลักแบบเรียลไทม์:

1. **💰 Revenue (รายได้รวม)**
   - แสดงรายได้รวมในช่วงเวลาที่เลือก
   - แสดงจำนวนคำสั่งซื้อ
   - คำนวณจากตาราง `orders` ที่มี status = 'paid'

2. **📊 Cost (ค่าใช้จ่ายโฆษณา)**
   - แสดงค่าใช้จ่ายโฆษณารวม
   - แสดงจำนวนคลิกทั้งหมด
   - คำนวณจากตาราง `ad_metrics`

3. **📈 ROI (Return on Investment)**
   - คำนวณ: `((Revenue - Cost) / Cost) * 100`
   - แสดงเป็นเปอร์เซ็นต์ (สีเขียวถ้าเป็นบวก, แดงถ้าลบ)
   - แสดง ROAS (Return on Ad Spend) ร่วมด้วย

4. **🎯 CTR (Click-Through Rate)**
   - คำนวณ: `(Clicks / Impressions) * 100`
   - แสดง CPM (Cost Per Mille) ร่วมด้วย

5. **✅ Conversions**
   - แสดงจำนวน conversion ทั้งหมด
   - แสดงอัตรา conversion rate

6. **👁️ Impressions**
   - แสดงจำนวนการแสดงผลโฆษณา

7. **👤 CAC (Customer Acquisition Cost)**
   - คำนวณ: `Total Cost / Total Clients`
   - แสดงต้นทุนต่อลูกค้า

8. **💎 LTV (Lifetime Value)**
   - คำนวณ: `Average Revenue per Client`
   - แสดงมูลค่าตลอดชีพของลูกค้า

#### 2. **AI Assistant Chat**
- **ฟังก์ชัน**: ให้ผู้ใช้ถามคำถามเป็นภาษาไทย
- **ตัวอย่างคำถาม**:
  - "ยอดขายเดือนนี้เท่าไร"
  - "จำนวนคลิก 7 วันล่าสุด"
  - "CTR เป็นเท่าไร"
  - "ROI และ ROAS เป็นเท่าไร"
- **การทำงาน**: 
  - ระบบจะ parse คำถามและดึงข้อมูลที่เกี่ยวข้อง
  - ตอบกลับเป็นภาษาไทยที่เข้าใจง่าย
  - แสดงคำถามแนะนำถ้าไม่เข้าใจคำถาม

#### 3. **Insights Panel**
- แสดง insights อัตโนมัติ:
  - ✅ **Positive Insights**: เมื่อ metric ดีขึ้น (เช่น CTR เพิ่มขึ้น)
  - ⚠️ **Warning Insights**: เมื่อมีปัญหา (เช่น ค่าใช้จ่ายเพิ่มแต่คลิกไม่เพิ่ม)
  - ℹ️ **Info Insights**: ข้อมูลที่น่าสนใจ (เช่น ช่องทางไหนทำงานดีที่สุด)

#### 4. **Recommendations Panel**
- แสดงคำแนะนำจาก AI:
  - 🔴 **High Priority**: ปัญหาที่ต้องแก้ไขทันที (เช่น CPA สูงเกินไป)
  - 🟡 **Medium Priority**: ควรพิจารณา (เช่น CTR ต่ำ, งบกระจายไม่ดี)
  - 🔵 **Low Priority**: ข้อเสนอแนะทั่วไป

#### 5. **Real-time Charts (24 ชั่วโมง)**
- **Area Chart**: แสดง Clicks และ Conversions แบบเรียลไทม์
- อัปเดตทุก 5 นาที
- แสดงข้อมูลชั่วโมงต่อชั่วโมง

#### 6. **Channel Performance Chart**
- **Bar Chart**: เปรียบเทียบแต่ละช่องทาง
- แสดง Cost และ Conversions ของแต่ละช่องทาง
- ช่วยให้เห็นช่องทางไหนใช้เงินมากและได้ผลลัพธ์เท่าไร

#### 7. **Trend Analysis Chart**
- **Line Chart**: แสดงแนวโน้มระยะยาว
- แสดง 4 metrics พร้อมกัน:
  - Clicks (สีน้ำเงิน)
  - Impressions (สีเขียว)
  - Cost (สีแดง, แกนขวา)
  - Conversions (สีเหลือง)
- สามารถเลือกช่วงเวลา: 7 วัน, 30 วัน, 90 วัน, 1 ปี

#### 8. **Anomaly Detection**
- ตรวจจับความผิดปกติในข้อมูล
- แสดงเมื่อ:
  - คลิกสูง/ต่ำผิดปกติ (เกิน ±20%)
  - ค่าใช้จ่ายสูง/ต่ำผิดปกติ
  - Conversion ลดลงผิดปกติ

---

## 🤖 AI Agent Features

### ไฟล์: `backend/controllers/aiAgentController.js`

### 1. **Natural Language Query** (`/api/ai/query`)

**ฟังก์ชัน**: ตอบคำถามเป็นภาษาไทย

**ตัวอย่างการทำงาน**:
```javascript
// คำถาม: "ยอดขายเดือนนี้เท่าไร"
// ระบบจะ:
1. ตรวจสอบคำว่า "ยอดขาย", "revenue", "sales"
2. Query ข้อมูลจากตาราง orders
3. คำนวณยอดรวม 30 วันล่าสุด
4. ตอบกลับ: "ยอดขายรวม 30 วันล่าสุด: 500,000 บาท จาก 150 คำสั่งซื้อ"
```

**รองรับคำถาม**:
- ยอดขาย/Revenue/Sales
- จำนวนคลิก/Clicks
- CTR
- ROI/ROAS
- และอื่นๆ

### 2. **Insights Generation** (`/api/ai/insights`)

**ฟังก์ชัน**: สร้าง insights อัตโนมัติจากข้อมูล

**การทำงาน**:
1. เปรียบเทียบข้อมูลปัจจุบันกับช่วงก่อนหน้า
2. คำนวณ % การเปลี่ยนแปลง
3. สร้าง insights:
   - ถ้า CTR เพิ่มขึ้น > 10% → Positive Insight
   - ถ้า CTR ลดลง > 10% → Warning Insight
   - ถ้า Cost เพิ่มแต่ Clicks ไม่เพิ่ม → Warning Insight
   - หาช่องทางที่ดีที่สุด → Info Insight

**Parameters**:
- `period`: ระยะเวลาที่ต้องการวิเคราะห์ (default: 30 วัน)

### 3. **Anomaly Detection** (`/api/ai/anomalies`)

**ฟังก์ชัน**: ตรวจจับความผิดปกติในข้อมูล

**การทำงาน**:
1. ดูข้อมูล 7 วันล่าสุด
2. คำนวณค่าเฉลี่ยของแต่ละ metric
3. ตรวจสอบว่าวันไหนเบี่ยงเบนเกิน threshold (default: 20%)
4. แสดงผลเป็นรายการ anomalies พร้อม:
   - วันที่
   - Metric ที่ผิดปกติ
   - ค่าที่เบี่ยงเบน
   - % การเบี่ยงเบน

**Parameters**:
- `threshold`: เปอร์เซ็นต์เบี่ยงเบนที่ถือว่าเป็น anomaly (default: 20%)

### 4. **Recommendations** (`/api/ai/recommendations`)

**ฟังก์ชัน**: ให้คำแนะนำการปรับปรุง

**ประเภทคำแนะนำ**:

1. **Optimization** (Priority: Medium)
   - เมื่อ CTR < 1% → แนะนำให้ปรับ ad copy หรือ targeting

2. **Cost** (Priority: High)
   - เมื่อ CPA > 100 บาท → แนะนำให้ทบทวนกลยุทธ์ bidding

3. **Strategy** (Priority: Medium)
   - เมื่องบ > 70% อยู่ที่ช่องทางเดียว → แนะนำให้กระจายงบ

---

## 📄 Reports System

### ไฟล์: `frontend-react/src/pages/Reports.jsx`

### สิ่งที่แสดง:

#### 1. **Filters Section**
- **วันที่เริ่มต้น**: เลือกวันที่เริ่มต้น
- **วันที่สิ้นสุด**: เลือกวันที่สิ้นสุด
- **ช่องทาง**: กรองตามช่องทาง (Google, Facebook, TikTok, etc.)
- **แคมเปญ**: กรองตามแคมเปญ

#### 2. **Summary Cards**
แสดงสรุปข้อมูล:
- Total Clicks
- Total Impressions
- Total Conversions
- Total Cost

#### 3. **Chart Visualization**
- **Bar Chart**: แสดงข้อมูลรายวัน
- แสดง Clicks, Impressions (แกนซ้าย)
- แสดง Cost (แกนขวา)

#### 4. **Data Table**
ตารางข้อมูลแบบละเอียด:
- วันที่
- ช่องทาง
- แคมเปญ
- คลิก
- Impressions
- Conversion
- ค่าใช้จ่าย
- CTR
- CPA

#### 5. **Export CSV**
- ปุ่ม "ส่งออก CSV"
- ดาวน์โหลดไฟล์ CSV พร้อมข้อมูลที่กรองแล้ว
- ไฟล์มี UTF-8 BOM สำหรับภาษาไทย

### Backend API: `/api/reports/detailed`
- ดึงข้อมูลรายละเอียดตาม filters
- คำนวณ CTR, CPA, CPM
- ส่งข้อมูลเป็น JSON

### Backend API: `/api/reports/export/csv`
- สร้างไฟล์ CSV จากข้อมูล
- ส่งกลับเป็น blob สำหรับดาวน์โหลด

---

## ⚙️ Settings Configuration

### ไฟล์: `frontend-react/src/pages/Settings.jsx`

### การตั้งค่าที่มี:

#### 1. **Refresh Interval**
- เลือกความถี่ในการรีเฟรชข้อมูลอัตโนมัติ
- ตัวเลือก: 1, 5, 10, 15, 30 นาที, 1 ชั่วโมง
- ข้อมูลจะอัปเดตตามช่วงเวลาที่กำหนด

#### 2. **KPI Thresholds**
ตั้งค่า threshold สำหรับการแจ้งเตือน:

- **CTR ต่ำสุด (%)**: จะแจ้งเตือนเมื่อ CTR ต่ำกว่าค่านี้
- **CPA สูงสุด (บาท)**: จะแจ้งเตือนเมื่อ CPA สูงกว่าค่านี้
- **ROI ต่ำสุด (%)**: จะแจ้งเตือนเมื่อ ROI ต่ำกว่าค่านี้
- **Conversion Rate ต่ำสุด (%)**: จะแจ้งเตือนเมื่อ Conversion Rate ต่ำกว่าค่านี้

#### 3. **Notifications**
เปิด/ปิดการแจ้งเตือน:

- **ส่งอีเมลแจ้งเตือน**: รับการแจ้งเตือนทางอีเมล
- **แจ้งเตือนเมื่อค่าเกิน threshold**: แจ้งเมื่อค่าใดๆ สูงกว่าที่กำหนด
- **แจ้งเตือนเมื่อค่าต่ำกว่า threshold**: แจ้งเมื่อค่าใดๆ ต่ำกว่าที่กำหนด

#### 4. **Appearance**
- **Theme**: สว่าง, มืด, อัตโนมัติ
- **Language**: ไทย, English

#### 5. **Save Settings**
- บันทึกการตั้งค่าใน localStorage
- แสดงข้อความยืนยันเมื่อบันทึกสำเร็จ

---

## 🛒 E-commerce Integration

### ไฟล์: `backend/controllers/ecommerceController.js`

### API Endpoints:

#### 1. **GET `/api/ecommerce/sales`**
ดึงข้อมูลยอดขาย:

**Parameters**:
- `startDate`, `endDate`: ช่วงวันที่
- `platform`: ช่องทาง (Shopee, Lazada, etc.)

**Response**:
```json
{
  "success": true,
  "data": {
    "sales": [
      {
        "date": "2024-01-01",
        "order_count": 50,
        "revenue": 50000,
        "avg_order_value": 1000
      }
    ],
    "topProducts": [
      {
        "name": "Product A",
        "price": 500,
        "units_sold": 100,
        "revenue": 50000
      }
    ],
    "stock": [
      {
        "name": "Product A",
        "stock_quantity": 50,
        "price": 500,
        "stock_status": "in_stock"
      }
    ]
  }
}
```

#### 2. **GET `/api/ecommerce/cart`**
ดึงข้อมูล Cart Analytics (โครงสร้างพร้อมสำหรับการเชื่อมต่อ API)

**Response**:
- Cart Abandonment Rate
- Average Cart Value
- Total Carts
- Converted Carts

### สิ่งที่แสดง:
- ยอดขายรายวัน
- สินค้าขายดี (Top 20)
- สถานะสต็อก (In Stock, Low Stock, Out of Stock)

---

## 🔌 Data Integration

### ไฟล์: `backend/services/dataIntegrationService.js`

### โครงสร้างสำหรับการเชื่อมต่อ:

#### 1. **Google Ads API**
- ฟังก์ชัน: `fetchGoogleAdsData()`
- ใช้: google-ads-api package
- ดึงข้อมูล: Campaigns, Clicks, Impressions, Cost, Conversions

#### 2. **Meta (Facebook/Instagram) Ads API**
- ฟังก์ชัน: `fetchMetaAdsData()`
- ใช้: facebook-nodejs-business-sdk
- ดึงข้อมูล: Ad Insights, Reach, Spend, Conversions

#### 3. **TikTok Ads API**
- ฟังก์ชัน: `fetchTikTokAdsData()`
- ใช้: TikTok Marketing API
- ดึงข้อมูล: Campaign Performance, Ad Metrics

#### 4. **Google Analytics 4 (GA4)**
- ฟังก์ชัน: `fetchGA4Data()`
- ใช้: @google-analytics/data package
- ดึงข้อมูล: Traffic, Conversions, Events

### API Endpoints:

#### **POST `/api/integration/sync`**
- Sync ข้อมูลจากทุกแพลตฟอร์ม
- ต้องมีสิทธิ์ Admin หรือ Manager
- รับ `startDate`, `endDate`, `clientId`
- บันทึกข้อมูลลงตาราง `ad_metrics`

#### **GET `/api/integration/status`**
- ดูสถานะการ sync ล่าสุด
- แสดงช่องทางและเวลาที่ sync ล่าสุด

### การ Transform ข้อมูล:
ฟังก์ชัน `transformToAdMetrics()`:
- แปลงข้อมูลจากแต่ละแพลตฟอร์มให้เป็นรูปแบบเดียวกัน
- บันทึกลงตาราง `ad_metrics` ด้วย format:
  - client_id
  - campaign
  - channel (Google, Facebook, TikTok, etc.)
  - clicks, impressions, conversions, cost
  - timestamp

---

## 🔧 Backend APIs

### Dashboard APIs (`/api/dashboard/*`)

#### 1. **GET `/api/dashboard/overview`**
ดึง KPI Overview

**Query Parameters**:
- `startDate`, `endDate`: ช่วงวันที่

**Response**:
```json
{
  "success": true,
  "data": {
    "adMetrics": {
      "clicks": 10000,
      "impressions": 500000,
      "conversions": 500,
      "cost": 50000,
      "ctr": "2.00",
      "cpm": "100.00",
      "conversionRate": "5.00",
      "cpa": "100.00"
    },
    "revenue": {
      "totalOrders": 500,
      "totalRevenue": 500000,
      "avgOrderValue": "1000.00"
    },
    "performance": {
      "roi": "900.00",
      "roas": "10.00",
      "ltv": "1000.00",
      "cac": "100.00"
    },
    "seo": {
      "totalKeywords": 100,
      "avgPosition": "5.5",
      "totalTraffic": 10000,
      "totalConversions": 50
    }
  }
}
```

#### 2. **GET `/api/dashboard/realtime`**
ดึงข้อมูล Real-time (24 ชั่วโมง)

**Query Parameters**:
- `hours`: จำนวนชั่วโมง (default: 24)

**Response**:
```json
{
  "success": true,
  "data": {
    "hourly": [
      {
        "hour": "2024-01-01 10:00:00",
        "clicks": 100,
        "impressions": 5000,
        "conversions": 5,
        "cost": 500
      }
    ],
    "current": {
      "clicks": 50,
      "impressions": 2500,
      "conversions": 2,
      "cost": 250
    }
  }
}
```

#### 3. **GET `/api/dashboard/channels`**
ดึงข้อมูล Channel Performance

**Response**: รายการช่องทางพร้อม metrics

#### 4. **GET `/api/dashboard/campaigns`**
ดึงข้อมูล Campaign Performance

**Query Parameters**:
- `startDate`, `endDate`
- `channel`: กรองตามช่องทาง

**Response**: รายการแคมเปญพร้อม metrics

#### 5. **GET `/api/dashboard/trends`**
ดึงข้อมูล Trend Analysis

**Query Parameters**:
- `startDate`, `endDate`
- `period`: day, week, month, hour

**Response**: ข้อมูลแนวโน้มตามช่วงเวลา

---

## 🎨 Frontend Components

### 1. **DashboardEnhanced Component**
- ใช้ Recharts สำหรับ visualization
- Responsive design (mobile, tablet, desktop)
- Auto-refresh ทุก 5 นาที
- Date range selector (7, 30, 90, 365 วัน)

### 2. **Reports Component**
- Filter system
- Data table with sorting
- Chart visualization
- CSV export

### 3. **Settings Component**
- Form validation
- LocalStorage persistence
- Toggle switches
- Save confirmation

### 4. **Layout Component**
- Navigation menu
- Responsive sidebar
- User info display
- Logout functionality

---

## 🔐 Security & Multi-tenant

### การควบคุมการเข้าถึง:

1. **Authentication**: ใช้ JWT token
2. **Authorization**: Role-based (admin, manager, viewer)
3. **Data Isolation**:
   - Viewer: เห็นเฉพาะข้อมูลของ client_id ของตนเอง
   - Manager/Admin: เห็นข้อมูลทั้งหมด

### ตัวอย่างการทำงาน:
```javascript
// ใน controller
const { client_id, role } = req.user;

let clientFilter = '';
let clientParams = [];
if (role !== 'admin' && role !== 'manager') {
  clientFilter = 'AND client_id = ?';
  clientParams = [client_id];
}
```

---

## 📊 Database Schema

### ตารางหลักที่ใช้:

1. **ad_metrics**: ข้อมูลการโฆษณา
   - client_id, campaign, channel
   - clicks, impressions, conversions, cost
   - timestamp

2. **orders**: ข้อมูลคำสั่งซื้อ
   - client_id, customer_name
   - total_amount, status
   - created_at

3. **seo_metrics**: ข้อมูล SEO
   - client_id, keyword, position
   - traffic, conversions
   - date

4. **products**: ข้อมูลสินค้า
   - client_id, name, price
   - stock_quantity

5. **alerts**: การแจ้งเตือน
   - client_id, metric, alert_condition
   - threshold, is_triggered

6. **users**: ผู้ใช้งาน
   - client_id, role (admin, manager, viewer)
   - email, password_hash

---

## 🚀 การใช้งาน

### 1. **Setup Backend**
```bash
cd backend
npm install
# สร้างไฟล์ .env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=rga_dashboard
JWT_SECRET=your_secret_key
PORT=5000
npm start
```

### 2. **Setup Frontend**
```bash
cd frontend-react
npm install
npm run dev
```

### 3. **เข้าถึงระบบ**
- Login: `http://localhost:5173/login`
- Dashboard: `http://localhost:5173/dashboard`
- Reports: `http://localhost:5173/reports`
- Settings: `http://localhost:5173/settings`

### 4. **การใช้งาน Dashboard**
1. เลือกช่วงวันที่ (7, 30, 90, 365 วัน)
2. ดู KPI Cards
3. ใช้ AI Assistant ถามคำถาม
4. ดู Insights และ Recommendations
5. ดู Real-time Charts
6. ตรวจสอบ Anomalies

### 5. **การใช้งาน Reports**
1. เลือกวันที่เริ่มต้นและสิ้นสุด
2. กรองตามช่องทางหรือแคมเปญ
3. ดู Summary Cards
4. ดู Chart และ Table
5. ส่งออก CSV

### 6. **การใช้งาน Settings**
1. ตั้งค่า Refresh Interval
2. กำหนด KPI Thresholds
3. เปิด/ปิด Notifications
4. เลือก Theme และ Language
5. บันทึกการตั้งค่า

---

## 📝 หมายเหตุสำคัญ

1. **Data Integration**: ต้องเพิ่ม API Keys จริงใน `.env` สำหรับการเชื่อมต่อแพลตฟอร์ม
2. **Real-time Updates**: ใช้ WebSocket สำหรับ real-time updates (โครงสร้างพร้อม)
3. **Export PDF**: ยังไม่ได้ทำ (มีเฉพาะ CSV)
4. **Email Notifications**: ต้องตั้งค่า email service
5. **Multi-language**: รองรับภาษาไทยเป็นหลัก (สามารถเพิ่มภาษาอื่นได้)

---

## 🎯 สรุปคุณสมบัติ

✅ **Dashboard Overview** - KPI Cards, Real-time Charts, Trends  
✅ **AI Agent** - Natural Language Query, Insights, Anomaly Detection, Recommendations  
✅ **Reports** - Detailed Reports, Filters, CSV Export  
✅ **Settings** - Configuration, Thresholds, Notifications  
✅ **E-commerce** - Sales Summary, Product Performance, Stock Status  
✅ **Data Integration** - โครงสร้างสำหรับ Google Ads, Meta Ads, TikTok, GA4  
✅ **Multi-tenant** - Data Isolation, Role-based Access  
✅ **Responsive** - ใช้งานได้บน Mobile, Tablet, Desktop  

---

**เอกสารนี้สร้างเมื่อ**: 2024  
**เวอร์ชัน**: 1.0.0  
**ผู้พัฒนา**: RGA Dashboard Team

